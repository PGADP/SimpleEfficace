# Règles UI — cascade de surcharge

Ce dossier est vide par défaut : les règles UI viennent de `~/.claude/se/rules/ui-rules.json` (le système).
Pour surcharger, copier ce fichier ici (`.planning/rules/ui-rules.json`) et l'ajuster.
La copie projet gagne toujours sur la version système.
