<!-- PHASES.md : registre append-only de tout ce qui a été LIVRÉ (phases et quicks). -->
<!-- Sans plafond de taille : il grandit avec le projet, c'est sa fonction. -->
<!-- Alimenté par /se-archive (transfert depuis ROADMAP.md et STATE.md), jamais réécrit. -->
<!-- UNE ligne par entrée, format fixe ci-dessous. Le détail vit dans le SUMMARY archivé. -->

# Phases et quicks livrés

## Phases

> Format : `- {YYYY-MM-DD} · {NN}-{slug} · {vX.Y} · {livré, 10 mots max} · [SUMMARY](_archive/phases/{NN}-{slug}/{fichier réel})`
> L'identifiant est `{NN}-{slug}`, le même que le dossier archivé. Pas de nom de code inventé.

- (aucune)

## Quicks

> Format : `- {YYYY-MM-DD} · quick {id} · {livré, 10 mots max} · [dossier](quick/{id}-{slug}/)`

- (aucun)
